import React from 'react';

const App: React.FC = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '50px' }}>
            <h1>Zoom Editor initial setup!</h1>
        </div>
    );
};

export default App;
